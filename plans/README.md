# Implementation Plans — CMS

Generated by the improve skill on 2026-07-28 (`plan` variant: CMS architecture handoff). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Source of truth (product/architecture):** `apps/portal/docs/cms-architecture.md` — decisions there override informal notes in these plans if they conflict.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| ---- | ----- | -------- | ------ | ---------- | ------ |
| 001 | CMS Convex — schema, deploy tokens, registerSchema, publish R2 | P1 | L | — | DONE |
| 002 | Package `@hezaerd/cms` + client site template | P1 | M | 001 | DONE |
| 003 | Portal — draft editing, preview JWT, publish UI | P1 | L | 001, 002 | DONE |
| 004 | CMS feature unlock + Needs Attention queues | P2 | S | 001, 003 | TODO |
| 005 | Pilot client site on Cloudflare Pages | P2 | L | 001–004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency graph

```
001 Convex backend
 ├─► 002 SDK + template (registerSchema CI calls 001 HTTP route)
 ├─► 003 Portal UI (queries/mutations from 001; preview consumes snapshot shape from 002)
 └─► 004 Feature unlock (needs publish/draft queries from 001; Mon site route from 003)
      └─► 005 CF Pages pilot (end-to-end: 001 publish → 002 Editable* → 003 Portal → 004 unlock)
```

## Dependency notes

- **002** requires **001** because `registerFields()` POSTs to `/cms/register-schema` and prod sites read `cms/{slug}/published/v{N}.json` produced by **001**'s publish path.
- **003** requires **001** (API) and **002** (field-key conventions + snapshot JSON shape). Image upload UI in **003** depends on R2 presign patterns from Files but uses the `cms/` key namespace defined in **001**.
- **004** should land after **003** so the Feature unlock CTA has a real Mon site destination; publish-ready Needs Attention needs draft-vs-published diff from **001**.
- **005** is the integration proof — do not start until **001–004** are DONE or explicitly scoped down with operator sign-off.

## Prior plans retired

Plans `001-portal-client-desk-chrome` and `002-portal-tanstack-query` were removed on 2026-07-28 to make room for this CMS sequence. Their work is already merged (status was DONE).

## Findings considered and rejected

- **Page builder / markdown / repeaters in v1:** rejected in `cms-architecture.md` grilling — text + image only.
- **Convex runtime reads on public sites:** rejected — prod reads baked R2 snapshot only.
- **Multi-tenant single renderer:** rejected — one custom repo per client.
- **Implementing Insights/analytics in this plan set:** out of scope — parallel feature, separate grilling.

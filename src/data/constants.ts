export const SECTION_IDS = {
	INTRO: "intro",
	WORK: "work",
	PROJECTS: "projects",
	CONNECT: "connect",
} as const;

export const SECTION_NAMES = [
	SECTION_IDS.INTRO,
	SECTION_IDS.WORK,
	SECTION_IDS.PROJECTS,
	SECTION_IDS.CONNECT,
] as const;

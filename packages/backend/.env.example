# Written by `bunx convex dev` — do not commit real values
CONVEX_DEPLOYMENT=
CONVEX_URL=
CONVEX_SITE_URL=

# Required on the Convex deployment for @convex-dev/workos-authkit:
#   bunx convex env set WORKOS_CLIENT_ID ...
#   bunx convex env set WORKOS_API_KEY ...
#   bunx convex env set WORKOS_WEBHOOK_SECRET ...
#
# Staging client id (Hezaerd / AuthKit app "hezaerd.com"):
#   client_01KY32DGMYJWDS29JRYGPE7ZGT
#
# Webhook endpoint (already created in Staging):
#   https://brainy-chinchilla-464.eu-west-1.convex.site/workos/webhook
#   events: user.created, user.updated, user.deleted
# Copy the signing secret from the WorkOS dashboard webhook detail page.
WORKOS_CLIENT_ID=
WORKOS_API_KEY=
WORKOS_WEBHOOK_SECRET=
